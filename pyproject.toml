[tool.pytest.ini_options]
markers = [
    "integration: tests that require real external services (Azure OpenAI)",
    "e2e: end-to-end browser tests via Playwright",
]
addopts = "-m 'not integration and not e2e'"
